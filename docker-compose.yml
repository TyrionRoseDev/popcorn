services:
  db:
    image: postgres:17
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: popcorn
      POSTGRES_PASSWORD: popcorn
      POSTGRES_DB: popcorn
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
